package org.ld4l.bib2lod.ontology;

public interface DatatypeProp extends OntologyProp {
    /*
     * No content, at least for now. Defined in order to enforce use of a 
     * datatype property with Entity Attributes, vs an object property with
     * related Entities. Different behaviors may surface later.
     */
}
